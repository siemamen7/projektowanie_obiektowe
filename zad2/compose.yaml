name: php_app

services:
  php:
    image: kprzystalski/projobj-php:latest
    working_dir: /home/student/projobj

    volumes:
      - .:/home/student/projobj
    ports:
      - "8000:8000"

    command: php -S 0.0.0.0:8000 -t public
